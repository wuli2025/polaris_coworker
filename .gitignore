node_modules
dist
dist-ssr
*.local

# 本机/部署密钥永不入库。仅提交无值模板。
.env
.env.*
!.env.example
!.env.server.example
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
src-tauri/target
src-tauri/gen
# 随安装包分发的运行时(uv / Python / Git Bash),约 227MB,由 scripts/fetch-runtimes.mjs
# 在 `tauri build` 前自动抓取,不入库。
# scripts/fetch-runtimes.mjs 抓的随包运行时(uv/python/git,约 225MB)。
# ⚠ 既然它不进仓库,**就不许出现在 tauri.windows.conf.json 的 bundle.resources 里** ——
#   tauri 的 build script 会逐条校验资源路径,少一个就整个 cargo 编译失败
#   (`resource path runtime\python doesn't exist`)。本机有这个目录时完全看不出来,
#   一到 CI 的干净检出必挂(v2.7.0 实测踩过)。要真启用随包运行时,得先让 CI 在 build 前
#   跑 fetch-runtimes.mjs;在那之前 doctor/bundled.rs 找不到它会回落到系统 uv/python/git。
src-tauri/runtime/
src-tauri/Cargo.lock
# 手机原生隧道插件的 Rust 构建产物(源码只留 Cargo.*/src)
mobile/native/*/target/
# e2e 跑 --shots 时落的截图,只用来人眼看一下,不入库
mobile/e2e/shots/

# Runtime outputs / local logs
.dev.log
output/
PolarisKB/output/
# 对话产物（运行时生成，落在工作文件夹/KB 根下的 conversations/，不该进仓库）
PolarisKB/conversations/
conversations/

# Claude workspace / temp files
.claude/
*.html
!index.html
!src-tauri/**/*.html

# 本地开发产物 / 截图 / 日志（不进仓库）
.shot-*.png
.dev-screenshot-*.png
.dev-server.log
.dev-run.log
/workspace/
PolarisKB/.polaris_seeded

# 本地工作/产物草稿目录（运行时生成，不进仓库）
/career-map-work/
/tmp/
/polaris-video-work/
/polaris-video-work-kbwiki/

# 生成的视频工作产物(大体积,勿提交)
/polaris-video-work-longoutput/
/polaris-video-work-zhiyuan/
/polaris-video-work-kbwiki/

# Docker 化本地产物（勿提交）
.claude-seed/
.docker-build.log
.docker-monitor.log
.docker-monitor-dry.log
docker-compose.override.yml

# UI 预览构建产物（并行会话生成,8GB+,勿提交）
src-tauri/target-ui-preview/

__pycache__/
*.pyc

# 本地运行日志 / 临时脚本草稿（运行时生成,勿提交）
_scratch/
src-tauri/*.log
_devrun.log
easytier-panic.log

# 本地 TLS 证书,勿入库
docker/relay/certs/

# 前端/构建工具的生成目录(勿入库,历史上误提交过,已 git rm --cached)
marketing-site/.next/
**/.next/
.wrangler/
.next/

# Codex/Superpowers 隔离开发工作树（目录自身由 git worktree 管理）
/.worktrees/

# 根目录临时产物(实验脚本/日志/截图/评测中间件),一律不入库; _collabtest/ 为可重放测试例外
/_*
!/_collabtest/
/.rustpull.log
/*.log
